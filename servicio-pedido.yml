spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/tiendavirtual
    username: root
    password: root
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
server:
  port: 9008
  servlet:
    context-path: /pedido
eureka:
  client:
   service-url:
     defaultZone: http://localhost:8761/eureka/
remote:
   url: http://servicio-producto/
   user: admin
   pwd: admin
db:
 security:
   driver: com.mysql.cj.jdbc.Driver
   url: jdbc:mysql://localhost:3306/springsecurity
   user: root
   password: root
logging:
 level:
   com.tu.paquete.ProductoClient: DEBUG
   feign.Logger: DEBUG

feign:
 client:
   config:
     default:
       loggerLevel: FULL
